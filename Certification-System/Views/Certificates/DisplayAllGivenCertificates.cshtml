@model IEnumerable<Certification_System.ViewModels.DisplayGivenCertificateViewModel>

@{
    ViewData["Title"] = "DisplayAllGivenCertificates";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Lista wszystkich nadanych certyfikatów</h1>

<p>
    @Html.ActionLink("Przyznaj certyfikat", "AddNewGivenCertificate", "Certificates", null, new { @class = "btn btn-primary" })

</p>

<div class="col-12 table-responsive">
    <table id="listGivenCertificates" class="table table-striped table-bordered display" style="width: 100%;" role="grid">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.GivenCertificateIndexer)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ReceiptDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ExpirationDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Certificate.CertificateIndexer) i @Html.DisplayNameFor(model => model.Certificate.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.User.FirstName) i @Html.DisplayNameFor(model => model.User.LastName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Course.CourseIndexer) i @Html.DisplayNameFor(model => model.Course.Name)
                </th>
                <th>
                    Operacje
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.GivenCertificateIndexer)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ReceiptDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ExpirationDate)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.Certificate.CertificateIndexer) @Html.DisplayNameFor(model => item.Certificate.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.User.FirstName) @Html.DisplayNameFor(model => item.User.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(model => item.Course.CourseIndexer) @Html.DisplayNameFor(model => item.Course.Name)
                    </td>
                    <td>
                        @Html.ActionLink("Szczegóły użytkownika", "UserDetails", "Users", new { userIdentificator = item.User.UserIdentificator }, new { @class = "btn btn-info" })
                        @Html.ActionLink("Edytuj certyfikat", "EditGivenCertificate", "Certificates", new { givenCertificateIdentificator = item.Certificate.CertificateIdentificator }, new { @class = "btn btn-warning" })
                        @Html.ActionLink("Szczegóły kursu", "EditGivenCertificate", "Certificates", new { givenCertificateIdentificator = item.Certificate.CertificateIdentificator }, new { @class = "btn btn-secondary" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {

            $('#listGivenCertificates').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.19/i18n/Polish.json"
                },
            }
            );
        });
    </script>
}
